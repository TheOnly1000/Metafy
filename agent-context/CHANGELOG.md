# MetaX V4 — CHANGELOG.md

- 2026-08-09 [V4 created from V3; package.json, main.js, config.json, renderer/index.html, renderer/app.js, renderer/style.css, splash.html, splash.js, tutorial.js, .gitignore] Initial V4: version 4.0.0, GitHub-release updater (TheOnly1000/MetaFy), MinIO local mode (s3_type + s3_endpoint), from-scratch embedded Python bootstrap (userData/pykeep), tutorial smoothing (no backdrop-filter, rAF re-measure, glow only animation).
- 2026-08-09 [main.js, package.json, dist] Installer/release test: added MSI target; updater prefers .exe asset and saves with real extension; built EXE+MSI for 4.0.0 (local install) and 4.0.1; reset GitHub main to placeholder "Create Purpose"; made repo public; released v4.0.1 with both installers uploaded; only installers live on GitHub, source remains local.
- 2026-08-09 [main.js, renderer/app.js, renderer/index.html, renderer/style.css, dist, GitHub release v4.0.1] Bugfix + copy: downloadFile now follows GitHub 302 redirects (update no longer writes empty file / stalls); apply-update silent-installs (/S for NSIS, msiexec /qn for MSI) then relaunches app; uploadToS3 detects MinIO console endpoint (port 9001 or /browser) and tells user to use API port 9000, normalizes trailing slash, prefixes errors with endpoint; UI "S3" wording replaced with "Bucket" ("Uploading to bucket...", "Uploaded N file(s) to bucket ..."), endpoint/path hints added; rebuilt 4.0.0 + 4.0.1 and re-pushed 4.0.1 assets.